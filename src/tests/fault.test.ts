// import { Direction, Fault, TypeOfMovement } from "../lib";

test('test fault class', () => {
    // let f = new Fault({strike: 45, dipDirection: Direction.NW, dip: 60})
    // f.setStriation({rake: 30, strikeDirection: Direction.NE, typeOfMovement: TypeOfMovement.I})
    // console.log(f)

    // f = new Fault({strike: 45, dipDirection: Direction.NW, dip: 60})
    // f.setStriation({rake: 30, strikeDirection: Direction.NE, typeOfMovement: TypeOfMovement.I})
    // console.log(f)

    // f = new Fault({strike: 45, dipDirection: Direction.NW, dip: 60})
    // f.setStriation({rake: 30, strikeDirection: Direction.NE, typeOfMovement: TypeOfMovement.I})
    // console.log(f)

    // f = new Fault({strike: 45, dipDirection: Direction.NW, dip: 60})
    // f.setStriation({rake: 30, strikeDirection: Direction.NE, typeOfMovement: TypeOfMovement.I})
    // console.log(f)

    // f = new Fault({strike: 45, dipDirection: Direction.NW, dip: 60})
    // f.setStriation({rake: 30, strikeDirection: Direction.NE, typeOfMovement: TypeOfMovement.I})
    // console.log(f)
    
    // // expect(sigma_1.radius).toEqual(1)
})
